<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MasterCodeMobile.Views.ListeForumsPage">
    <ContentPage.Content>
        <ScrollView>
        <StackLayout>
            <Label Text="Forums" HorizontalOptions="Center" FontSize="Large"/>
            <ListView x:Name="CategoriesListeView"
                      ItemsSource="{Binding Forums}"
                      HasUnevenRows="True"
                      RefreshCommand="{Binding LoadForumsCommand}"
                      IsPullToRefreshEnabled="True"
                      IsRefreshing="{Binding IsBusy,Mode=OneWay}"
                      CachingStrategy="RecycleElement">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                                <StackLayout>
                                    <StackLayout  BackgroundColor="#99ff5e" Margin="2" >

                                        <Label Text="{Binding Sujet}"
                                    LineBreakMode="NoWrap"
                                    Style="{DynamicResource ListItemStyle}"
                                    FontSize="20"
                                    Margin="0,20"
                                       
                                    HorizontalOptions="Center">

                                        </Label>
                                        <Label Text="{Binding DatePublication}" TextColor="Blue" FontSize="Small" HorizontalOptions="Center">

                                        </Label>

                                    </StackLayout>
                                </StackLayout>
                                
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>